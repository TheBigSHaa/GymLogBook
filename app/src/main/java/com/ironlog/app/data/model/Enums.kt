package com.ironlog.app.data.model

enum class DayType {
    POWER,
    HYPERTROPHY,
    CONDITIONING,
}

enum class EquipmentType {
    BARBELL,
    DUMBBELL,
    BODYWEIGHT,
    KETTLEBELL,
    BAND,
    NONE,
}

enum class RepUnit {
    REPS,
    STEPS,
    SECONDS,
    PER_SIDE,
    PER_LEG,
}

